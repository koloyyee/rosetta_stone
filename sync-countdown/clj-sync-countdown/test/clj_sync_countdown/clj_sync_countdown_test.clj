(ns clj-sync-countdown.clj-sync-countdown-test
  (:require [clojure.test :refer :all]
            [clj-sync-countdown.clj-sync-countdown :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
